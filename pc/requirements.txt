# Tier 2 (PC / RTX server) runtime deps — install into the wes-pc venv.
# See docs/setup.md for the authoritative notes (venv location, voice model,
# the two version pins and *why* they exist).
#
#   python -m pip install -r pc/requirements.txt
#
# Unpinned where any recent release works; pinned only where a specific version
# is required (documented inline). CI installs the lighter test subset instead —
# see requirements-dev.txt.

flask
anthropic
prometheus-client
pyyaml
faster-whisper
piper-tts
pychromecast
discord.py

# --- Pins (see docs/setup.md) ------------------------------------------------
# 4.8.0 segfaults (0xC0000005) on model load on the PC.
ctranslate2==4.4.0
# 1.27.0's DLL init fails; piper needs onnxruntime.
onnxruntime==1.20.1
