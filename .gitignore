.venv/
.venv-pc/
__pycache__/
*.pyc
photos/
hailort.log
*.un~
*~
tests/.pytest_cache/
tests/eval/fixtures/

# Local monitoring-stack settings (the WSL gateway address, Grafana password).
# observability/.env.example is the committed template.
observability/.env

# Claude Code local settings (machine-specific permissions)
.claude/settings.local.json
